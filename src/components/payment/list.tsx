import TransactionList from '../subscription/paymentTab'

const Index = () => {
  return (
    <div>
      <TransactionList embeddingMode={false} enableSearch={true} />
    </div>
  )
}

export default Index
